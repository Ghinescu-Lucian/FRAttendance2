# Moodle FaceAttendance API configuration
# Keep this file private. Do not commit it to Git.

FACE_ATTENDANCE_MARK_ENABLED=true
FACE_ATTENDANCE_MARK_URL=https://192.168.0.154/mod/faceattendance/api/mark.php
FACE_ATTENDANCE_ROSTER_URL=https://192.168.0.154/mod/faceattendance/api/roster.php?cmid=18
FACE_ATTENDANCE_CMID=18
FACE_ATTENDANCE_SECRET=1234
FACE_ATTENDANCE_SOURCE=camera-lab-1
FACE_ATTENDANCE_TIMEOUT_SECONDS=3.0

# For your local HTTPS/IP setup this is false because the certificate is probably self-signed
# or not valid for 192.168.0.154. Use true only when you have a trusted HTTPS certificate.
FACE_ATTENDANCE_VERIFY_TLS=false
